为多态基类声明virtual析构函数

经由基类的指针或引用调用一个方法时，如果该方法是虚的，那么C++会根据指针或引用指向的实际对象类型来调用方法，如果方法是非虚的，那么C++会根据指针或引用的类型来调用方法。
当一个派生类对象经由一个基类指针被删除时，如果析构函数不是虚的，那么只会调用基类的析构函数，导致派生类对象的成员只有部分被删除。


所以，继承一个不带虚析构函数的基类极易引起内存泄露，这样的类包括标准库自带的string以及所有STL容器等。